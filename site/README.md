# Configorama Docs Site

Nextra 4 App Router documentation site for Configorama.

## Local Development

```sh
npm install
npm run dev
```

## Validation

```sh
npm run validate
```

The site deploys to Netlify from the repository root with `base = "site"` and `publish = ".next"`.
