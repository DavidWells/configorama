# YAML that is technically valid but has unusual structure
---
# Document marker

# Unusual but valid YAML constructs
weird_key_with_underscores_and_numbers_123: value

# String that looks like variable syntax but uses invalid type (no resolver named 'literal')
not_a_variable: "no variables here just plain text"

# Nested structure with unusual formatting
nested:
  level1:
    level2: ${self:weird_key_with_underscores_and_numbers_123}

# Empty values
empty_value:
null_value: null

# Multiline string
multiline: |
  This is a multiline
  string with ${self:stage}
  variable inside

# Multiline folded
folded: >
  This is folded
  into one line ${self:nested.level1.level2}

stage: test
