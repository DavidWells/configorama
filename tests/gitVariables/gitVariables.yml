########################
# Values Types
########################

normalKey: value

number: 1

# owner/name
repo: ${git:repo}
repository: ${git:repository}

# owner/name
owner: ${git:owner}
repoOwner: ${git:repoOwner}
repoOwnerDashed: ${git:repo-owner}

# Url
url: ${git:url}
repoUrl: ${git:repoUrl}
repoUrlDashed: ${git:repo-url}

dir: ${git:dir}

# Branch
branch: ${git:branch}

# Commits
commit: ${git:commit}

sha1: ${git:sha1}

message: ${git:message}

# Remotes
remote: ${git:remote}
remoteDefined: ${git:remote('origin')}
remoteDefinedNoQuotes: ${git:remote(origin)}

# Tags
tag: ${git:tag}
describe: ${git:describe}

gitTimestamp: ${git:timestamp('../../package.json')}