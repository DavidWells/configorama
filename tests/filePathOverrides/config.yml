# Config file that references env.yml
envVars: ${file(./env.yml)}
specificKey: ${file(./env.yml):dbHost}

# Text file reference
readmeContent: ${text(./readme.txt)}

# Without ./ prefix
envVarsNoDotSlash: ${file(env.yml)}
