{
  "variable": {
    "region": {
      "description": "AWS region",
      "type": "string",
      "default": "us-west-2"
    },
    "app_name": {
      "description": "Application name",
      "type": "string",
      "default": "myapp"
    }
  },
  "locals": {
    "full_name": "${var.app_name}-app"
  }
}
